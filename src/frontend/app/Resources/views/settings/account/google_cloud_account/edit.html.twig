{% extends 'layout/admin.html.twig' %}

{% block title %}
    {% if entity.id is null %} Add Google cloud account{% else %} Edit Google cloud account{% endif %}
{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">
        <li>Settings</li>
        <li><a href="{{ path(controllerDescription.routes['list']) }}">Google cloud accounts</a></li>
        <li class="active">{{ block('title') }}</li>
    </ol>
{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-9">

            <div class="box box-primary" role="presentation">
                <div class="box-header">
                    <h3 class="box-title">Google cloud account</h3>
                </div>
                <div class="box-body">
                    {{ form_start(form) }}
                    {{ form_row(form.name, {'label': 'Name'}) }}
                    {{ form_row(form.serviceAccount, {'label': 'Service account', 'attr': {'rows': 10} }) }}
                    {{ form_rest(form) }}

                    <button class="btn btn-primary">Save</button>
                    <a href="{{ path(controllerDescription.routes['list']) }}" class="btn btn-default">Cancel</a>
                    {{ form_end(form) }}
                </div>
            </div>

        </div>
    </div>
{% endblock %}